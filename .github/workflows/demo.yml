name: Demo Workflow
on: workflow_dispatch
jobs:
  print: 
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: echo Hello actions
      - name: Print other
        run: |
          echo first line
          echo second line
